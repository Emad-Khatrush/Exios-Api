// Must mirror the theme ids in Exios-Client's src/constants/orderThemes.ts so a
// customer's chosen color always resolves to the same gradient on the client.
exports.ORDER_THEME_IDS = ['emerald', 'ocean', 'sunset', 'rose', 'violet', 'amber', 'slate', 'ink'];
